const express = require("express");

// TODO: require routers and assign them to

const app = express();

// TODO: app.use("/", router)

// TODO: not found handler

// TODO: error handler

module.exports = app;
